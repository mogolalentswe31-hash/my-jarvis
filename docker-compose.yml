version: '3.8'

services:
  jarvis:
    build: .
    container_name: my-jarvis
    ports:
      - "5000:5000"
    environment:
      - FLASK_ENV=development
      - DEBUG=true
    volumes:
      - .:/app
    env_file:
      - .env
    command: python main.py
